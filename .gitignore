# macOS
.DS_Store

# Personal data — never commit, even on a private fork unless you edit this file
resumes/

# Job tracker data — local only, contains personal application history
job_tracker.csv
startup_tracker.csv

# Outreach contact logs — local only, contains names + LinkedIn URLs
outreach/

# Startup outreach contact DB — local only, contains emails + handles
startup_outreach/

# Application folders — local only if you create them
applications/

# Editor / IDE
.idea/
.vscode/
*.swp
*~

# Python (in case you add scripts)
__pycache__/
*.pyc
.venv/
venv/

# Claude Code local settings
.claude/settings.local.json
